//write a loop makes seven calls to console.log to output the following traingle
//Be useful to know that you can find the length of a string by writing .length after it
for(let i = "#"; i.length < 8; i += "#") {
	console.log(i);
}
